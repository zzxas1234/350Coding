// Code Modular Structure

// Headers

// adc config

// calibration (if key in --> unlock controls) (set prev state ->>>> set system stage)

// set system state (set fsr, set usds) (call read functions)

// read fsr --> adc (use example)

// read range finder --> adc (use example)

//cancel


int main(void){

   
    while(1){

    // set system state (set fsr, set usds) (call read functions)

    // execute action required
    }
    
}
